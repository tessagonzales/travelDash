<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
    <link rel="stylesheet" href="https://bootswatch.com/4/sandstone/bootstrap.min.css">
    <link rel="stylesheet" href="../style.css">
  </head>
  <body>
    <% for(let i=0; i<airline.length; i++){ %>
     <h1>All upcoming flights for <%= airline[i].name %> </h1>
    <% } %><BR><BR>

    <h3>Ok here ya flights so far:</h3>
    <% for(let i=0; i<flight.length; i++) { %>
    <p>DEPART: <%= flight[i].start %><BR>
    ARRIVAL: <%= flight[i].destination %></p>
      <% } %><BR><BR><BR>


<h4>Create a New Flight</h4>
   <form action="/airline" method="post">
     <input type="text" name="start" placeholder="Boarding Location">
     <input type="text" name="destination" placeholder="Destination">
     <button class="btn btn-primary" type="submit">Create Flight</button>
   </form>
  </body>
</html>
