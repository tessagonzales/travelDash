<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
    <link rel="stylesheet" href="https://bootswatch.com/4/sandstone/bootstrap.min.css">
    <link rel="stylesheet" href="../style.css">
  </head>
  <body class="trips">
    <div class="container-fluid">
      <div class="row">
    <nav class="navbar nav-trips navbar-inverse">
        <h1 class="title-thing">Welcome, <%= user.name %>!</h1>
      </nav>
    </div>

    <div class="row">
    <h1 class="col-md-8 nav-trips offset-md-1">Your Trips</h1>
  </div>
    <% for(let i=0;i<trip.length;i++){ %>
      <p><%= trip[i].title %></p>
      <p><%= trip[i].description %></p>
      <% for(let i=0;i < flight.length;i++){ %>
       <p><%= flight[i].destination %></p>
      <% } %>
      <% } %>
    </div>
<h3 class="nav-trips">Add a new trip</h3>
    <form action="/trips/create" method="post">
      <input type="text" name="title" placeholder="Where To?">
      <input type="text" name="description" placeholder="About Trip">
      <select name="flight_id">
               <option value="default">Select Existing Flight</option>
                 <% for(let i=0;i < flight.length;i++){ %>
               <option value="<%= flight[i].id %>"><%= flight[i].destination %></option>
               <% } %>
      </select>
      <button class="btn btn-primary" type="submit">Add Trip!</button>

    </form>
  </div>
  </body>
</html>
